//Array ...allows to hold multiple values .rather variable which has 
// capacity of holding single values only .

//it works like same as variable 
class ArrayLearning
{
    static void Main(string[] args)
    {
        //direct value put
       // string [] namesOfCars = { "A", "B", "C", "D" };

        //change the array value 

        //namesOfCars[1] = "n";

        int[] myNumbers= new int[5];

        myNumbers[0] = 12;
        myNumbers[1] = 13;
        myNumbers[2] = 14;
        myNumbers[3] = 15;
        myNumbers[4] = 16;


        Console.WriteLine(myNumbers[0]);

        Console.ReadLine();
            
    }
    

}